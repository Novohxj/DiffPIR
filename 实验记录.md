# 实验记录

## 2026-04-28 Inpainting Baseline 初始化

目标：准备 DiffPIR 的 inpainting baseline 最小复现环境，不修改算法代码。

### 权重放置

- 目标目录：`model_zoo/`
- 需要权重：`diffusion_ffhq_10m.pt`
- 备注：仓库 `.gitignore` 已忽略 `*.pt`，因此权重不会进入本次提交

### 基本配置

配置来源：`configs/inpaint.yaml`

- `task: inpaint`
  指定任务分支为图像修复，`main_ddpir.py` 会进入 inpainting 的 mask 与数据一致性更新逻辑。
- `seed: 42`
  随机种子，用来固定 mask 生成、噪声采样和整体复现顺序。
- `noise_level_img: 0`
  观测图像额外添加的 AWGN 强度，配置文件里单位是 0 到 255 标度；代码读取后会除以 255。这里设为 0，表示只做缺失像素修复，不再额外加观测噪声。
- `noise_level_model: noise_level_img`
  名义上表示给扩散模型匹配的噪声水平；但当前代码会直接执行 `config.noise_level_model = config.noise_level_img`，因此它实际上不能独立于 `noise_level_img` 调参。
- `model_name: diffusion_ffhq_10m`
  指定使用的预训练扩散先验权重文件名，实际查找路径为 `model_zoo/diffusion_ffhq_10m.pt`。
- `testset_name: demo_test`
  指定输入图像目录，实际路径为 `testsets/demo_test/`。
- `num_train_timesteps: 1000`
  扩散模型原始训练时使用的总时间步数，也决定推理时噪声日程的长度。
- `iter_num: 20`
  实际推理采样步数，也就是本次最小复现的 NFE 级别。
- `iter_num_U: 1`
  每个外层 timestep 内重复执行内层更新的次数。这里为 1，表示每个 timestep 只更新一次。
- `batch_size: 16`
  推理 DataLoader 的 batch 大小。当前 `demo_test` 只有 5 张图，因此实际会一次跑完。
- `save_L: true`
  是否保存带 mask 的观测图像。
- `save_E: true`
  是否保存最终恢复结果。
- `lambda_: 1.0`
  数据一致性强度的基础系数。代码里它会进入 `rho_t = lambda * sigma^2 / sigma_k^2`，从而控制每个 timestep 的数据项权重。
- `zeta: 1.0`
  反推到下一步时注入随机性的系数。代码在回注噪声公式里同时使用 `sqrt(1-zeta)` 和 `sqrt(zeta)`；`zeta=1` 表示更偏向额外随机噪声分量。
- `sub_1_analytic: true`
  是否使用解析形式的数据子问题更新。对 inpainting 来说，这会走 `(mask*y + rho*x0) / (mask + rho)` 这条闭式更新。
- `log_process: false`
  是否打印更细的中间过程日志。
- `ddim_sample: false`
  是否使用 DDIM 采样；这里为 `false`，因此底层走 `diffusion.p_sample`。
- `model_output_type: pred_xstart`
  扩散模型每一步返回什么量。这里取预测的干净图 `x0`，也是当前 inpainting baseline 的主路径。
- `generate_mode: DiffPIR`
  指定采用 DiffPIR 而不是 `repaint` 或 `vanilla`。代码也对 inpainting 限制了这个字段只能是这三者之一。
- `skip_type: quad`
  20 个采样点如何从 1000 个原始 timestep 中选出来。`quad` 表示按平方根间隔取点，前期更密、后期更稀。
- `eta: 0.0`
  与 DDIM/回注噪声有关的参数。当前配置下虽然公式里仍会出现它，但由于 `eta=0`，对应项不额外引入这部分噪声。
- `guidance_scale: 1.0`
  数据一致性修正的混合强度。`x0 <- x0 + guidance_scale * (x0_p - x0)`，这里 1.0 表示直接采用完整修正。
- `n_channels: 3`
  按 RGB 三通道图像处理。
- `cwd: ''`
  作为路径前缀使用。当前为空串，因此默认相对当前仓库根目录去找 `model_zoo/`、`testsets/`、`results/`。
- `calc_LPIPS: true`
  除了 PSNR 外，同时计算 LPIPS 感知指标。
- `beta_start: 0.0001`
  线性噪声日程起点。
- `beta_end: 0.02`
  线性噪声日程终点。
- `noise_init_img: max`
  初始噪声起点设置。这里为 `max`，代码会直接把 `t_start` 设成 `num_train_timesteps - 1`，即从最高噪声时刻开始反推。
- `skip_noise_model_t: false`
  是否根据观测噪声水平跳过低噪声区间的模型更新。这里为 `false`，因此 `noise_model_t = 0`，不额外跳过。
- `sf: 1`
  该字段主要为统一三类任务接口保留。对 inpainting 没有超分意义，基本可视为占位参数。
- `mask_name: 'gt_keep_masks/face/000000.png'`
  如果 `load_mask=true`，就从这个相对路径读取固定 mask。
- `load_mask: false`
  是否读取固定 mask。这里为 `false`，所以实际运行时不会用 `mask_name`，而是在线随机生成 mask。
- `mask_type: random`
  mask 生成模式。这里使用随机像素缺失，而不是方框缺失。
- `mask_len_range: [128, 129]`
  只在 `box` 或 `extreme` 类型 mask 下有意义，用来控制方框尺寸范围。当前 `mask_type=random`，所以这个字段本次最小复现里基本不生效。
- `mask_prob_range: [0.5, 0.5]`
  随机 mask 的缺失概率范围。这里上下界一样，表示每次都按 50% 的像素缺失率采样。
- `save_progressive_mask: false`
  是否额外保存带 mask 叠加的逐步恢复过程图。当前关闭。

### 最小运行命令

```bash
python main_ddpir.py --opt configs/inpaint.yaml
```

### 环境与数据可用性检查

#### 数据检查

- 已确认 `testsets/demo_test/` 存在 5 张输入图像：
  - `69037.png`
  - `69133.png`
  - `69367.png`
  - `69887.png`
  - `69929.png`
- 已确认 inpainting 默认权重存在：`model_zoo/diffusion_ffhq_10m.pt`
- 已确认该权重不是空文件，文件大小为 `374417833 bytes`
- 同目录存在 `._diffusion_ffhq_10m.pt`，文件大小 `4096 bytes`，这是 macOS 附属元数据文件，不是程序实际加载的权重

#### Python 环境检查

- 当前运行位置：AutoDL 服务器容器
- 当前工作目录：`/root/autodl-tmp/DiffPIR`
- 当前用户：`root`
- 当前主机名：`autodl-container-a2d545ac4b-317d1c40`
- 默认 `python` 路径：`/root/miniconda3/bin/python`
- 默认 `python` 版本：`Python 3.12.3`
- 已创建新的 Conda 环境：`diffpir`
- 环境路径：`/root/miniconda3/envs/diffpir`
- 环境 Python 版本：`Python 3.9.25`
- 创建命令：

```bash
conda create -n diffpir python=3.9 -y
```

- 选择 `python=3.9` 的原因：`requirements.txt` 固定了 `torch==1.13.0+cu117`、`torchvision==0.14.0+cu117`、`torchaudio==0.13.0`，该组合不适合默认的 Python 3.12。
- 依赖安装命令：

```bash
/root/miniconda3/envs/diffpir/bin/pip install -r requirements.txt
/root/miniconda3/envs/diffpir/bin/pip install PyYAML
```

- `requirements.txt` 安装已成功完成。
- 额外安装 `PyYAML==6.0.3`，原因是 `requirements.txt` 未列出该依赖，但运行 `configs/inpaint.yaml` 需要 `yaml` 模块。
- 依赖导入验证命令：

```bash
/root/miniconda3/envs/diffpir/bin/python -c "import torch, cv2, yaml, lpips; print('python_ok'); print('torch=' + torch.__version__); print('cuda_available=' + str(torch.cuda.is_available())); print('cv2=' + cv2.__version__); print('yaml=' + yaml.__version__); print('lpips_ok')"
```

- 验证结果：

```text
python_ok
torch=1.13.0+cu117
cuda_available=False
cv2=4.7.0
yaml=6.0.3
lpips_ok
```

### Inpainting 最小复现结果

- 运行命令：

```bash
/root/miniconda3/envs/diffpir/bin/python main_ddpir.py --opt configs/inpaint.yaml
```

- 结果目录：

```text
results/demo_test_ip_DiffPIR_random_diffusion_ffhq_10m_sigma0.0_NFE20_eta0.0_zeta1.0_lambda1.0
```

- 输出文件：
  - `69037_diffusion_ffhq_10m.png`
  - `69133_diffusion_ffhq_10m.png`
  - `69367_diffusion_ffhq_10m.png`
  - `69887_diffusion_ffhq_10m.png`
  - `69929_diffusion_ffhq_10m.png`
  - `demo_test_ip_DiffPIR_random_diffusion_ffhq_10m_sigma0.0_NFE20_eta0.0_zeta1.0_lambda1.0.log`

- 单图指标：
  - `69037.png`: PSNR `32.2654 dB`, LPIPS `0.1177`
  - `69133.png`: PSNR `31.1361 dB`, LPIPS `0.1101`
  - `69367.png`: PSNR `34.9773 dB`, LPIPS `0.1457`
  - `69887.png`: PSNR `32.3448 dB`, LPIPS `0.0849`
  - `69929.png`: PSNR `32.4868 dB`, LPIPS `0.1018`

- 平均指标：
  - Average PSNR: `32.6421 dB`
  - Average LPIPS: `0.1120`

### 当前状态

- 代码未修改
- baseline inpainting 配置已登记
- 已确认 `model_zoo/diffusion_ffhq_10m.pt` 存在
- 已确认 `testsets/demo_test/` 数据存在
- 已配置并验证 Conda 环境 `/root/miniconda3/envs/diffpir`
- 已完成 DiffPIR inpainting 最小复现
- 结果已保存到 `results/demo_test_ip_DiffPIR_random_diffusion_ffhq_10m_sigma0.0_NFE20_eta0.0_zeta1.0_lambda1.0`
